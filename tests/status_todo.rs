#[test]
#[ignore = "live maintenance events are time dependent"]
fn status_endpoint_todo() {
    // API: GET /api/v5/system/status
    // STATUS: TODO — live maintenance events are time dependent and may return no rows.
    todo!("exercise status filtering when a deterministic maintenance fixture is available");
}
